<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dev Skill Details</title>
</head>
<body>
  <h1>Dev Skill</h1>
  <% if(error) { %>
    <h2><%= error %></h2>
  <% } else { %>
    <h2>Skill #<%= devskill._id %></h2>
    <h3><%= devskill.text %></h3>
    <p>mastered: <%= devskill.mastered ? 'Yes' : 'No' %></p>
  <% } %>
  <form action="/devskills/<%= devskill._id %>?_method=DELETE"
      class="delete-form"
      method="POST">
      <button type="submit">Remove Skill</button>
  </form>
    <a href="/devskills">Go back</a>
</body>
</html>